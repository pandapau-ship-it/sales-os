# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Env (secrets) — .env.local ist bereits über *.local abgedeckt
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code local (per-clone) settings — not versioned
.claude/settings.local.json

# Supabase CLI local cache (per-clone, vom CLI bei db push/link verändert) — nicht versioniert
supabase/.temp/

# Deno-Lockfile (lokales Edge-Tooling, kein Projekt-Deno-Config-Anker)
deno.lock
